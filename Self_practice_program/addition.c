// add two entered integer values

#include<stdio.h>

void main()
{
        int a, b, add;
        printf("Enter first number: ");
        scanf("%d", &a);
        printf("Enter second number: ");
        scanf("%d", &b);
        add = a + b;
        printf("add of %d and %d is %d", a, b, add);

}