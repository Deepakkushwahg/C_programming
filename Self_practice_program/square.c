/* 
first number raised to 
the power second number
*/

#include<stdio.h>

void main()
{
        int a, square;
        scanf("%d", &a);
        square = a*a;
        printf("%d*%d is %d", a, a, square);

}