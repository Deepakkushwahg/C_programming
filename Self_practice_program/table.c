// display the table of an number 

#include<stdio.h>
#include<math.h>

void main()
{
int num, i, tab;
printf("Enter the number: ");
scanf("%d", &num);
for ( i = 1;i=10;i++)
tab = num * i;
printf("table is %d", tab);

}